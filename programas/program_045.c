#include <stdio.h>
#include <stdlib.h>

int main ( ) {
    int i , soma = 0 ;
    while ( i <= 10) {
        soma = soma + i ; i ++;

    }
    printf ("Soma = %d \n", soma )
    ;
    system ( "pause" ) ;
    return 0;
};  }
